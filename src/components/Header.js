import { Link } from "react-router-dom";
import './Header.css'

export default function Header (props){
  return (
    <div className="header">
        <div class="jumbotron">Have a little crypto with your coffee...</div>
    </div>
  );
};